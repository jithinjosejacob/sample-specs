# Add Pet — Create a new pet ✅

**Purpose:** Verify that `POST /pet`

**Tests:**
1. Assert response status is 200 and JSON body contains `id`. // Spec 1
2. Assert response status is 200 and JSON body contains `name`, and `status`. // Spec 1
3. Assert response status is 200 and JSON body contains  `status`. // Spec 1

4. (Optional) GET `/pet/{id}` and assert the same fields are returned. // Spec 2


